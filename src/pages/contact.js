import Contact from "@/components/Contact";
import Hero from "@/components/Hero";

export default function contact() {
  return (
    <div>
        <Hero heading='Contato' message='Submit the form below for more work and quotes' bg={'h-[600px]'}/>
        <Contact />
    </div>
  )
}
